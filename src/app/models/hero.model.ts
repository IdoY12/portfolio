/**
 * Hero section data model
 * CUSTOMIZE: Update these properties to match your personal information
 */
export interface HeroData {
  name: string;
  title: string;
  tagline: string;
  image?: string; // Optional profile image URL
}
